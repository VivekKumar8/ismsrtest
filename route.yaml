apiVersion: v1
kind: Route
metadata:
  name: microsvcruntime
  namespace: {PROJECT_NAME}
spec:
  port:
    targetPort: 5555-tcp
  to:
    kind: Service
    name: microsvcruntimesrv
    weight: 100
  wildcardPolicy: None

